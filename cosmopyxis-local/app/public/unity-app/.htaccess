# Cosmopyxis Unity WebGL Configuration
# VERBOSE: Proper MIME types for Unity WebGL files

# Unity WebGL MIME types
AddType application/wasm .wasm
AddType application/javascript .js
AddType application/octet-stream .data
AddType application/octet-stream .symbols.json

# Enable compression for Unity files
<IfModule mod_deflate.c>
    AddOutputFilterByType DEFLATE application/wasm
    AddOutputFilterByType DEFLATE application/javascript
    AddOutputFilterByType DEFLATE application/octet-stream
</IfModule>

# Cache control for Unity assets
<IfModule mod_expires.c>
    ExpiresActive On
    ExpiresByType application/wasm "access plus 1 month"
    ExpiresByType application/javascript "access plus 1 month"
    ExpiresByType application/octet-stream "access plus 1 month"
</IfModule>

# Security headers
Header always set X-Frame-Options "SAMEORIGIN"
Header always set X-Content-Type-Options "nosniff"
